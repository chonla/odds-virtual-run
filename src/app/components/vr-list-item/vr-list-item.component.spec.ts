import { VrListItemComponent } from './vr-list-item.component';

describe('VrListItemComponent', () => {
  let component: VrListItemComponent;

  it('should create', () => {
    component = new VrListItemComponent();
    expect(component).toBeTruthy();
  });
});
