import { StatsBoxComponent } from './stats-box.component';

describe('StatsBoxComponent', () => {
  let component: StatsBoxComponent;

  beforeEach(() => {
    component = new StatsBoxComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
